<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MarwanAlgo | التداول الآلي بالذكاء الاصطناعي</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #0b0e14;
            --card-bg: #151a23;
            --accent-green: #00e676;
            --accent-blue: #2979ff;
            --text-main: #ffffff;
            --text-muted: #9e9e9e;
            --border-color: #232a36;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Cairo', sans-serif;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            line-height: 1.6;
        }

        /* Navbar */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.5rem 5%;
            background: rgba(11, 14, 20, 0.9);
            border-bottom: 1px solid var(--border-color);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            color: var(--text-main);
        }

        .logo span {
            color: var(--accent-green);
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
        }

        .nav-links a {
            color: var(--text-muted);
            text-decoration: none;
            transition: 0.3s;
        }

        .nav-links a:hover {
            color: var(--accent-green);
        }

        .btn-primary {
            background: linear-gradient(45deg, var(--accent-green), #00b0ff);
            color: #000;
            padding: 0.7rem 1.5rem;
            border-radius: 8px;
            font-weight: 700;
            text-decoration: none;
            transition: 0.3s;
            border: none;
            cursor: pointer;
        }

        .btn-primary:hover {
            opacity: 0.9;
            transform: translateY(-2px);
        }

        /* Hero Section */
        .hero {
            padding: 5rem 5%;
            text-align: center;
            background: radial-gradient(circle at center, #131c2e 0%, var(--bg-color) 70%);
        }

        .hero h1 {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 1rem;
        }

        .hero h1 span {
            color: var(--accent-green);
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--text-muted);
            max-width: 700px;
            margin: 0 auto 2rem;
        }

        /* Video Showcase */
        .video-container {
            max-width: 800px;
            margin: 3rem auto;
            background: var(--card-bg);
            border: 2px solid var(--border-color);
            border-radius: 16px;
            padding: 1rem;
            box-shadow: 0 0 30px rgba(0, 230, 118, 0.1);
        }

        .video-placeholder {
            width: 100%;
            height: 400px;
            background: #000;
            border-radius: 12px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--text-muted);
            font-size: 1.2rem;
        }

        /* Features */
        .features {
            padding: 4rem 5%;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            background: var(--card-bg);
            padding: 2rem;
            border-radius: 12px;
            border: 1px solid var(--border-color);
            transition: 0.3s;
        }

        .feature-card:hover {
            border-color: var(--accent-green);
        }

        .feature-card h3 {
            color: var(--accent-green);
            margin-bottom: 0.5rem;
        }

        /* Pricing */
        .pricing {
            padding: 4rem 5%;
            text-align: center;
        }

        .pricing-grid {
            display: flex;
            justify-content: center;
            gap: 2rem;
            flex-wrap: wrap;
            margin-top: 2rem;
        }

        .price-card {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            padding: 2.5rem;
            width: 300px;
            text-align: center;
            position: relative;
        }

        .price-card.popular {
            border-color: var(--accent-green);
            box-shadow: 0 0 20px rgba(0, 230, 118, 0.2);
        }

        .price {
            font-size: 2.5rem;
            font-weight: 800;
            margin: 1rem 0;
            color: var(--accent-green);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 2rem;
            border-top: 1px solid var(--border-color);
            color: var(--text-muted);
        }
    </style>
</head>
<body>

    <!-- القائمة العلوية -->
    <nav>
        <div class="logo">Marwan<span>Algo</span></div>
        <ul class="nav-links">
            <li><a href="#about">عن البوت</a></li>
            <li><a href="#video">شرح الفيديو</a></li>
            <li><a href="#pricing">الأسعار</a></li>
        </ul>
        <a href="#pricing" class="btn-primary">اشترك الآن</a>
    </nav>

    <!-- واجهة البداية -->
    <section class="hero">
        <h1>تداول بذكاء وسرعة مع <span>MarwanAlgo</span></h1>
        <p>خوارزمية تداول أوتوماتيكية تعمل على مدار 24/7 لتحليل السوق وتنفيذ أفضل الصفقات بدقة عالية.</p>
        <a href="#pricing" class="btn-primary">جرّب البوت اليوم</a>

        <!-- مكان شرح الفيديو -->
        <div class="video-container" id="video">
            <div class="video-placeholder">
                🎬 [مكان وضع فيديو فيديو شرح البوت وإثبات الأرباح]
            </div>
        </div>
    </section>

    <!-- المميزات -->
    <section class="features" id="about">
        <div class="feature-card">
            <h3>⚡ تنفيذ أوتوماتيكي</h3>
            <p>يعمل البوت على جهازك بشكل آلي كامل دون الحاجة للتواجد أمامه طوال اليوم.</p>
        </div>
        <div class="feature-card">
            <h3>🔒 حماية وإدارة مخاطر</h3>
            <p>إمكانية تحديد أهداف الأرباح ووقف الخسارة لكل صفقة لحماية رأس مالك.</p>
        </div>
        <div class="feature-card">
            <h3>⏱️ تفعيل زمني محدد</h3>
            <p>نظام تراخيص ذكي يرتبط باشتراكك ويُحدث أوتوماتيكياً من لوحة تحكمك.</p>
        </div>
    </section>

    <!-- خطط الأسعار -->
    <section class="pricing" id="pricing">
        <h2>خطط الاشتراك</h2>
        <div class="pricing-grid">
            <div class="price-card">
                <h3>اشتراك شهري</h3>
                <div class="price">$49 <span>/شهر</span></div>
                <p>تفعيل لمشترك واحد لمدة 30 يوم</p>
                <br>
                <button class="btn-primary" onclick="alert('سندمج بوابة الدفع في الخطوات القادمة!')">احصل عليه الآن</button>
            </div>
            <div class="price-card popular">
                <h3>اشتراك سنوي</h3>
                <div class="price">$399 <span>/سنة</span></div>
                <p>تفعيل كامل + دعم فني خاص</p>
                <br>
                <button class="btn-primary" onclick="alert('سندمج بوابة الدفع في الخطوات القادمة!')">احصل عليه الآن</button>
            </div>
        </div>
    </section>

    <footer>
        <p>جميع الحقوق محفوظة © 2026 MarwanAlgo</p>
    </footer>

</body>
</html>